<h1>Burgers</h1>

<img src="../public/assets/img/burger.png">

{{!-- box to type in new burgers --}}
<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <button type="submit" id="submit">Add Burger</button>
</form>

{{!-- uneaten/eaten burgers; float right when eaten? button to eat inbetween --}}
<h2>Burgers that have not yet been eaten!</h2>


<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burger-block devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been eaten and are thus no longer available to be eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burger-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>